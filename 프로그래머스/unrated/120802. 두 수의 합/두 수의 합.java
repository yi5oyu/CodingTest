class Solution {
    public int solution(int num1, int num2) {
        int min = -50000;
        int max = 50000;
        int answer = -1;
        if(min <= num1 && num1 <= max && min <= num1 && num1 <= max ){
            answer = num1 + num2;
        }
        return answer;
    }
}